"""Pure, I/O-free domain packages for LedgerCopilot.

`domain`, `validation`, `policy` and `reconciliation` must be testable without a
database or network. No SQLAlchemy and no LLM calls in here.
"""
